import * as React from "react";
import * as Types from "./webflow_modules/types";

declare function ElementCustomerReview(props: {
  lastStarPercentage?: string;
  logo?: Types.Asset.Image;
  reviewCounter?: React.ReactNode;
  reviewNumber?: React.ReactNode;
  testimonialName?: React.ReactNode;
  testimonialText?: React.ReactNode;
}): React.JSX.Element;
